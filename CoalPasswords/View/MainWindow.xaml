<Window x:Class="CoalPasswords.PasswordsMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoalPasswords"
        mc:Ignorable="d" MinHeight="600" MinWidth="1000" WindowStyle="None" Title="Coal Passwords" Style="{StaticResource WindowBaseStyle}" Height="720" Width="1280" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="{Binding ResizeBorderThickness}" CaptionHeight="{Binding TitleHeight}" CornerRadius="{Binding WindowCornerRadius}"></WindowChrome>
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="8*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"></RowDefinition>
                <RowDefinition Height="7*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" VerticalAlignment="Center">
                <Button Background="Transparent" Style="{StaticResource SideMenuButton}">
                    All Passwords
                </Button>
                <Button Background="Transparent" Style="{StaticResource SideMenuButton}">
                    Credit Cards
                </Button>
            </StackPanel>
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <Label Background="Transparent" Style="{StaticResource SideMenuLabel}" FontWeight="Bold">CATEGORIES</Label>
                <Button Background="Transparent" Style="{StaticResource SideMenuButton}">Mail</Button>
                <Button Background="Transparent" Style="{StaticResource SideMenuButton}">Shopping</Button>
                <Button Background="Transparent" Style="{StaticResource SideMenuButton}">Work</Button>
                <Button Background="Transparent" Style="{StaticResource SideMenuButton}">Entertainment</Button>
            </StackPanel>
            <!--<Button Grid.Row="2"></Button>-->
        </Grid>
        <Grid Grid.Column="1" x:Name="ItemsCol" >
            <ListView ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemTemplate="{StaticResource ListViewTemplate}" BorderThickness="0" Background="Transparent" ItemsSource="{Binding PassRecords}" Foreground="Black" >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel></WrapPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </Grid>
        <Button Grid.Column="1" Style="{StaticResource GradientButton}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="15 15" Command="{Binding AddTestCommand}">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="50"></Setter>
                </Style>
            </Button.Resources>
            <Button.Content>Add</Button.Content>
        </Button>
    </Grid>
    <!--<ListBox Background="Transparent" BorderThickness="0" ItemsSource="{Binding PassRecords}">
                <ListBox.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource ScrollBarStyle}"></Style>
                </ListBox.Resources>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"></Setter>
                                <Setter Property="BorderBrush" Value="White"></Setter>
                                <Setter Property="FontWeight" Value="Bold"></Setter>
                            </Trigger>
                        </Style.Triggers>
                        <Style.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"></SolidColorBrush>
                        </Style.Resources>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="x:Type local:PasswordRecord">
                        <UniformGrid Columns="2" Margin="0 10">
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <LinearGradientBrush>
                                        <GradientStop Color="#667eea" Offset="0.2"></GradientStop>
                                        <GradientStop Color="#764ba2" Offset="0.8"></GradientStop>
                                    </LinearGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <StackPanel>
                                <Label Foreground="White" FontFamily="Lato" FontSize="14" Content="{Binding Title}"></Label>
                                <Label Foreground="White" FontFamily="Lato" FontSize="14" Content="{Binding Website}"></Label>
                            </StackPanel>
                        </UniformGrid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>-->
</Window>